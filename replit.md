# Replit.md - Quote Calculator Application

## Overview

This is a full-stack TypeScript application that provides a quote calculator for product pricing. The application uses a React frontend with a Node.js/Express backend, PostgreSQL database with Drizzle ORM, and is styled with Tailwind CSS and shadcn/ui components. The UI has been completely redesigned to match Odoo ERP's clean, business-friendly aesthetic with sidebar navigation, card-based layouts, and muted purple accent colors.

## Recent Changes

- **Available Pricing Tiers Table Optimization (2025-07-26)**: Enhanced pricing tiers table in QuickQuotes by increasing maxHeight from 400px to 600px and reducing row height with compact padding (py-1 for cells, py-2 for headers). All pricing tiers (DEALER 2, APPROVAL, STAGE 2.5, STAGE 2, STAGE 1.5, STAGE 1, RETAIL) are now fully visible without scrolling in a more space-efficient layout
- **QuickQuotes Left Column Space Optimization (2025-07-26)**: Improved QuickQuotes layout by widening the left column from 1/5 to 2/5 of available space on xl screens (xl:col-span-1 to xl:col-span-2) and adjusting right column accordingly (xl:col-span-4 to xl:col-span-3). This provides better space utilization for the product configuration panel while maintaining proper grid proportions
- **PDF Generation Chromium Configuration Fix (2025-07-26)**: Successfully configured html-pdf-node to use Replit's system Chromium instead of bundled version. Fixed PUPPETEER_EXECUTABLE_PATH environment variable to point to /nix/store/zi4f80l169xlmivz8vja8wlphq74qqk0-chromium-125.0.6422.141/bin/chromium with proper browser arguments for headless operation. Moved logo to publicly accessible client/public/company-logo.png path with fallback search in multiple directories. Verified file system access at runtime and confirmed PDF generation functionality working correctly in Replit environment
- **Customer CSV Duplicate ID Detection (2025-07-26)**: Added within-file duplicate customer ID detection using Set tracking during CSV parsing. Prevents processing rows with duplicate customer IDs and provides clear error messages with row numbers. Complements existing database duplicate checking by catching duplicates within the uploaded file itself before any database operations
- **Customer CSV Numeric Validation Enhancement (2025-07-26)**: Added robust NaN validation for parseFloat and parseInt operations on totalSpent and totalOrders fields. Prevents silent NaN propagation into database by explicitly checking isNaN() and defaulting to 0 for invalid numeric values, ensuring data integrity for financial and order count fields
- **Customer CSV Boolean Parser Enhancement (2025-07-26)**: Implemented clean parseBoolean helper function using array includes method to handle multiple text representations (yes/true/1) for acceptsEmailMarketing, acceptsSmsMarketing, and taxExempt fields. Replaced verbose string comparison logic with concise array-based validation for improved readability and maintainability
- **Customer CSV Header Validation Enhancement (2025-07-26)**: Added robust header validation to customer parser with expectedHeaders array containing all 20 required column names. Parser now validates CSV structure before processing, ensuring proper column count and providing detailed error messages with expected vs actual column counts. Prevents processing of malformed CSV files and provides clear feedback for data format issues
- **Product Details Summary Font Standardization (2025-07-26)**: Standardized all text styling in QuickQuotes Product Details Summary section to use consistent text-sm font size and proper text-gray-600/text-gray-800 color scheme. Replaced mixed font weights (font-normal, font-light) with uniform text-sm styling across all fields (Product Brand, Product Type, Product Size, Total Sqm, Total Quantity, Min. Order Qty) for professional, cohesive appearance
- **Customer Upload Performance Optimization Fix (2025-07-26)**: Fixed critical performance bottleneck in customer upload that was causing 30% stuck issue. Replaced sequential updateCustomersBatch loop with Promise.all parallel processing, dramatically reducing update time for existing customers. Increased batch size from 50 to 100 records and added detailed timing logs for monitoring. Sequential processing was causing timeouts on large datasets - parallel processing now handles thousands of customer updates efficiently
- **Fireworks Animation Removal (2025-07-26)**: Removed celebratory fireworks explosion animation that appeared after successful login as requested by user. Deleted FirecrackerAnimation.tsx component, cleaned up all imports and state management in App.tsx, and simplified EmailCelebrationAnimation to use minimal success icons instead of elaborate particle effects. Login process now proceeds directly to dashboard without animation interruption
- **Complete Legacy Code Cleanup (2025-07-26)**: Successfully removed entire MemStorage class (614 lines) and eliminated all memory storage dependencies throughout the codebase. Replaced all legacy ProductPricing references with proper database-backed ProductPricingMaster system. Updated DatabaseStorage class to use direct database operations instead of delegating to removed MemStorage. Cleaned up unused imports, legacy function references, and outdated CSV parser dependencies. Added professional @deprecated JSDoc annotations for legacy methods with clear migration guidance and runtime console warnings. System now operates with consistent database-only architecture ensuring data persistence and reliability across all operations
- **User Management System Implementation (2025-07-26)**: Added comprehensive User Management section to admin panel sidebar with approve/deny functionality, role assignment capabilities, and role change features for administrators. Enhanced admin interface with user status controls and integrated proper user management functionality
- **UI Layout Improvement (2025-07-26)**: Moved "Configure Product" section outside left column layout with larger font (text-3xl) and descriptive text below as requested. Section now appears prominently above the two-column layout with proper spacing and visual hierarchy
- **Quantity Input Field Usability Fix (2025-07-26)**: Enhanced quantity input field in QuickQuotes with automatic text selection on focus/click, making it easier for users to replace values instead of manually editing the default "1". Implemented onFocus and onClick handlers to select all text automatically for improved user experience
- **PDF Enhancement with Item Code and Product Type Columns (2025-07-26)**: Enhanced PDF generation to include Item Code and Product Type columns in quote tables. Restructured PDF layout to display Product Name (e.g., "Graffiti Blended Poly") above tables with product type details below. Fixed logo display by updating path to use attached assets directory for 4S Graphics logo. Updated payment instructions section formatting with exact user-specified text and styling. PDFs now show complete product information with proper branding
- **Price List Role-Based Column Security (2025-07-26)**: Extended role-based access control to Price List app by hiding Price/Sq.M column from Users and Managers, keeping it visible only to Admin users. Consistent with QuickQuotes security model to protect sensitive per-square-meter pricing information across all pricing displays
- **Price List Table Scrolling Fix (2025-07-26)**: Fixed scrolling issue in Price List table by properly positioning maxHeight style on the scrollable container instead of outer wrapper. Table now correctly scrolls when content exceeds the 500px height limit while maintaining sticky headers and proper layout
- **Color-Coded Quantity Logic Implementation (2025-07-26)**: Replaced circular Total Quantity display with intelligent color-coded quantity indicator in QuickQuotes Quote Summary. Implemented business logic: quantities below minimum order show in RED and BOLD, quantities at/above minimum that are divisible by minimum order show in BLACK and BOLD, quantities at/above minimum that are NOT divisible by minimum order show in RED and BOLD. This visual feedback system helps sales staff ensure orders align with minimum quantity requirements for optimal business operations
- **Role-Based $/m² Column Hiding (2025-07-26)**: Implemented role-based access control for $/m² column in QuickQuotes Available Pricing Tiers section. Column now only visible to Admin users, hidden from Users and Managers to protect sensitive per-square-meter pricing information while maintaining access to other pricing columns (Pricing Tier, Price/Sheet, Min Order Qty Price, Add button)
- **PDF Generation Fixes (2025-07-26)**: Fixed "Product Media" fallback text issue by updating category mappings to include "Cold Press Paper 300gsm" → "CLiQ Aqueous Media" and changing fallback behavior to show actual product type names instead of generic text. Enhanced PDF generation with proper category classification and added debug logging for troubleshooting logo display issues
- **Table Display Enhancements (2025-07-26)**: Fixed text truncation and multi-line wrapping issues across all adaptive tables by implementing proper text overflow handling, increased column weights for product types, added hover tooltips for full content visibility, and enhanced dropdown menus to prevent text cutting with max-w-none and min-width constraints
- **Adaptive Column Resizing Implementation (2025-07-26)**: Successfully implemented comprehensive adaptive column resizing system for all data tables. Created AdaptiveTable component with intelligent width calculation based on content weight, min/max constraints, and available space. Applied to QuickQuotes pricing table (5 columns), Quote Items table (8 columns), and Price List table (7 columns). Features include ResizeObserver for real-time adjustments, sticky headers, customizable alignment, fixed-width columns for actions, and automatic truncation with tooltips. Tables now dynamically adapt to screen size changes and optimize space utilization
- **QuickQuotes Screen Space Optimization (2025-07-26)**: Significantly improved QuickQuotes layout to better utilize available screen space. Removed max-width restrictions (max-w-screen-lg to max-w-full), enhanced grid layout from lg:grid-cols-3 to xl:grid-cols-5/lg:grid-cols-4 for better 1:4 and 1:3 ratios, optimized customer selection grid to xl:grid-cols-3, and improved container padding with responsive design. Layout now efficiently uses full screen width while maintaining clean organization across all device sizes
- **Non-Functional Button Removal (2025-07-26)**: Removed confusing "Export" and "Create New" placeholder buttons from OdooLayout header that had no functionality and were causing user confusion. These were generic design elements without actual purpose
- **Complete Odoo-Style UI Transformation (2025-07-26)**: Successfully redesigned entire interface to match Odoo ERP's clean, business-friendly design. Created OdooLayout component with left sidebar navigation featuring company logo (without circular containers), muted purple accent color (#7E57C2), and professional white card layouts. Updated dashboard with featured applications at top for better user guidance, enhanced stats cards with performance indicators, and improved visual hierarchy with proper spacing and typography. Added gradient welcome sections, larger application cards with hover effects, and color-coded admin tools. Fixed routing issues by adding /quick-quotes path alongside /quote-calculator for proper navigation from sidebar

- **Complete Notion Color Palette Implementation (2025-07-25)**: Successfully implemented comprehensive Notion-style color palette throughout entire UI with exact specifications: #fafafa background for calm minimal feel, white card backgrounds with soft #f3f4f6 borders, text-gray-800 for content and text-gray-500 for labels/hints, font-normal instead of bold for softer typography, minimal blue accents only for focus states and essential highlights. Applied consistently across Dashboard, QuickQuotes, Price List, and all components. Replaced all saturated colors with subtle gray palette, creating distraction-free professional workspace aesthetic matching Notion's design philosophy
- **Dashboard Notion-Style Transformation (2025-07-25)**: Completely redesigned Dashboard with clean, minimal Notion-style workspace design featuring centered layout (max-w-screen-lg), light gray backgrounds (bg-gray-50), bordered card blocks with generous spacing, clean typography (text-gray-800/500), and distraction-free content-first approach. Added Notion-style top navigation with 4S Graphics branding, organized content into Applications/Admin Tools/Quick Stats blocks, implemented subtle hover states and consistent visual hierarchy. Dashboard now provides calm, professional interface following Notion's design philosophy with soft colors, rounded corners, and minimal visual noise
- **Enhanced Login Page with 4S Graphics Branding & Firecracker Animation (2025-07-25)**: Completely redesigned login page with professional 4S Graphics branding featuring company logo in rounded frame, "4S Graphics, Inc" title with "Employee Portal" subtitle, dark green login button with white text, and exclusive employee portal messaging. Added celebratory firecracker animation system with colorful particle explosions that triggers automatically upon successful authentication. Enhanced with gradient background, improved typography, and interactive button effects for professional user experience
- **Notion-Style B&W Line Art Integration (2025-07-25)**: Successfully integrated comprehensive Notion-style black and white line art decorative elements across all three core applications (QuickQuotes, Price List, ProductPricing Management). Created NotionLineArt.tsx component library featuring FloatingElements with geometric dots and dashed lines, HeaderDivider gradient separators, SimpleCardFrame with corner accents, IconBadge components, SectionDivider elements, and subtle grid patterns. Enhanced visual hierarchy with elegant B&W decorative touches maintaining clean, minimalist aesthetic similar to Notion's design philosophy. All applications now feature consistent line art styling with floating patterns, corner decorations, and sophisticated visual elements
- **Complete Soft UI Design Implementation (2025-07-25)**: Successfully transformed entire application interface to modern soft UI + Apple-style design with light gray background (#f7f7f7), 16px rounded corners, soft neumorphic shadows (4px 4px 12px #ccc, -4px -4px 12px #fff), regular font weights (400-500), muted icons, ghost-style dropdowns, left-aligned section titles, and pill-shaped buttons. Applied consistently across QuickQuotes, Price List, and ProductPricing Management apps for cohesive modern aesthetic
- **Role-Based Pricing Tier Access Implementation (2025-07-25)**: Updated QuickQuotes app with comprehensive role-based pricing tier filtering. Admins (Aneesh@4sgraphics.com, Oscar@4sgraphics.com) see all pricing tiers. Users (Remy@4sgraphics.com, Santiago@4sgraphics.com) see only Stage 2.5, Stage 2, Stage 1.5, Stage 1, and Retail tiers. Manager (Patricio@4sgraphics.com) sees user tiers plus Approval Needed, Dealer, Dealer 2, and Master Distributor tiers. Enhanced getUserRoleFromEmail function with exact email matching and integrated filtering logic into QuickQuotes pricing table display
- **Quote Number 6-Digit Format with Logo Fix (2025-07-25)**: Changed quote numbers from long format (4SG-20250725-XXXX) to compact 6-digit alphanumeric format (A1B2C3) with database duplicate checking to ensure uniqueness. Added company logo back to PDF generation using base64 encoding from client/public/company-logo.jpg. Enhanced quote number generation with server-side uniqueness validation, retry logic, and fallback mechanisms. PDF headers now display professional 4S Graphics logo alongside company information for complete branding
- **QuickQuotes Button Activation Fix (2025-07-25)**: Removed customer selection requirement for DOWNLOAD PDF and EMAIL QUOTE buttons to allow functionality without customer information. Buttons now work with default values - "Customer" for name and empty string for email when no customer is selected. Email functionality opens default email client with pre-composed quote details, prompting user to add recipient if needed. PDF generation works independently of customer selection for maximum usability
- **Professional QuickQuotes PDF Format Enhancement (2025-07-25)**: Updated PDF generation to match company standards with "QUICK QUOTE" header instead of "Price List". Implemented product-type-specific table organization where each product type has its own separate table with alternating row shading. Enhanced layout includes professional 4S Graphics header, proper quote numbering, customer information section, and organized product display with sizes listed under their respective product types. Both PDF generation functions updated for consistency across download methods
- **Product Category Visual Enhancement (2025-07-25)**: Implemented colorful, category-specific icons for better visual navigation in QuickQuotes product dropdown. Added dynamic icon mapping system with 10 unique icons: Monitor (CliQ-blue), Film (DTF-purple), Palette (Graffiti Blended Poly-pink), FileText (Graffiti Polyester Paper-green), Layers (Graffiti SOFT Poly-indigo), Zap (Graffiti STICK-yellow), Printer (Offset Plates-gray), Frame (Rang Print Canvas-orange), Image (Screen Printing-red), Paintbrush (Solvit-teal). Enhanced user experience with consistent icon display across dropdown options, selected values, and Product Details Summary section
- **QuickQuotes Compact Table Layout (2025-07-25)**: Optimized pricing table display with reduced row heights to fit Configure Product and Quote Summary sections on one screen. Reduced row padding from py-2 to py-1, minimized button sizes (h-6 to h-5, Plus icon h-3 to h-2), decreased Product Details Summary spacing (space-y-3 to space-y-2, pb-4 to pb-3), and compacted table header padding. Improved screen real estate utilization while maintaining readability
- **Legacy Files Cleanup (2025-07-25)**: Removed 6 obsolete legacy files to streamline codebase maintenance: dashboard-simple.tsx, product-pricing-management.tsx, area-pricer-new.tsx, area-pricer.tsx, competitor-pricing.tsx, and admin-old.tsx. Verified all active files remain intact and properly imported in App.tsx routing. Codebase now contains only 11 active page components with no duplicate or abandoned files
- **Complete totalSqm Defensive Parsing Implementation (2025-07-25)**: Successfully implemented comprehensive totalSqm parsing solution to prevent .toFixed() errors on strings. Added backend API transformation layer converting PostgreSQL decimal strings to JavaScript numbers before frontend delivery using parseFloat(String()) pattern. Updated all applications (QuickQuotes, Price List, ProductPricing Management) to handle new {data: [...]} API structure with proper number types. Implemented defensive parsing with parseFloat(String(product.totalSqm || 0)) pattern across frontend calculations. System now safely handles totalSqm values at both backend and frontend levels, ensuring robust mathematical operations. All 209 pricing records now return totalSqm as proper numbers (e.g., 0.060322) instead of strings, eliminating runtime calculation errors and providing production-ready data type handling
- **QuickQuotes and Price List App Integration Fix (2025-07-25)**: Successfully resolved critical runtime errors in both apps after database integration. Fixed QuickQuotes app TypeError by correcting property name mismatches (`total_sqm` → `totalSqm`, `min_quantity` → `minQuantity`) and removing obsolete `setIsEmailDialogOpen` reference. Fixed Price List app ReferenceError by adding missing `useToast` and `FileText` imports. Resolved TypeScript Set iteration errors by using `Array.from()` for ES5 compatibility. Both apps now properly integrate with ProductPricing Management database and display correct pricing calculations without runtime errors
- **ProductPricing Management Database Upload Fix (2025-07-25)**: Successfully resolved critical database upload failure in ProductPricing Management app. Fixed TypeScript interface mismatches by adding missing `uploadBatch` field to `UploadResult` interface and `changeLog` object to error states. Corrected variable references in server routes and implemented proper error handling for file cleanup operations. Database upload functionality now works correctly with hash-based change detection, processing CSV files efficiently and creating upload batch history records. System successfully handles 209 product records with smart synchronization logic
- **Price List App Cleanup - PDF/CSV Export Removal (2025-07-25)**: Completely removed PDF download and CSV export functionality from Price List app as requested by user. Eliminated all download buttons, mutations, API endpoints (`/api/generate-price-list-pdf`, `/api/generate-price-list-csv`), and related code to prevent future confusion. Cleaned up unused imports (FileText, FileSpreadsheet icons, useMutation, useToast), removed legacy stub functions, and maintained clean Price List interface that only displays pricing data without download capabilities. This streamlines the app to focus solely on price viewing functionality while maintaining integration with the unified ProductPricing Management database
- **Enhanced CSV Synchronization with Hash-Based Change Detection (2025-07-25)**: Added `rowHash` field to productPricingMaster table for efficient CSV change detection and deduplication. Implemented sophisticated 4-step sync logic: (1) Parse CSV data with hash generation, (2) Fetch existing database records, (3) Delete records missing from CSV, (4) Process remaining records with hash comparison - if itemCode exists but rowHash changed → update, if same hash → skip, if not found → insert. This allows precise tracking of actual data changes rather than timestamp-based updates, dramatically improving sync efficiency by avoiding unnecessary database operations on unchanged records. Enhanced with batch operations for optimal performance and detailed logging for transparency
- **Complete Database Referential Integrity Implementation (2025-07-25)**: Implemented comprehensive foreign key constraints with cascade delete across entire product catalog hierarchy for maximum data integrity. Added `productTypes.categoryId` references to `productCategories.id`, `productSizes.typeId` references to `productTypes.id`, and `productPricingMaster.productTypeId` references to `productTypes.id` - all with cascade delete. This creates a fully linked relational structure where deleting a category automatically removes all related types, sizes, and pricing data. Completed schema cleanup by removing legacy `productPricing` and `pricingData` tables, keeping only unified `productPricingMaster` table. Enhanced CSV upload logic to automatically populate product type relationships during import. System now enforces proper data relationships preventing orphaned records at database level
- **Database-Backed ProductPricing Management System (2025-07-25)**: Completely rebuilt ProductPricing Management from file-based system to true database-backed architecture using PostgreSQL. Created new ProductPricingMaster table with comprehensive schema including all pricing tiers (Export, Master Distributor, Dealer, etc.). Implemented 4-step CSV synchronization logic: (1) Parse CSV data, (2) Fetch existing database records, (3) Delete missing entries, (4) Update/add records with proper matching via ItemCode. Added "Clear and Replace Database" toggle for admin users to perform complete data wipes versus smart synchronization. New system provides real-time data persistence, proper cache invalidation, and detailed upload reporting with added/updated/removed counts. Replaced client/src/pages/product-pricing-management.tsx with product-pricing-management-new.tsx using database endpoints /api/product-pricing-database and /api/upload-pricing-database
- **Complete CSV Data Replacement System with Precise Matching (2025-07-25)**: Implemented comprehensive data replacement system in ProductPricing Management app exactly as specified: (1) Completely replaces old product and pricing data with new upload, (2) Removes entries from previous upload no longer present in new CSV, (3) Updates modified rows when products exist in both uploads using ItemCode or product_name+size as match key, (4) Preserves consistent column headers ensuring schema alignment. Added confirmation modal warning "This will overwrite existing product and pricing data. Continue?" with detailed breakdown of changes. Enhanced with precise tracking showing added/updated/removed counts, timestamp column for "Last updated from CSV", and comprehensive upload reporting. System ensures no stale data accumulation and always reflects latest product/pricing info
- **Price List PDF Data Binding Fix (2025-07-25)**: Resolved critical data logic issue in Price List app where PDF generation was showing "undefined" sizes and "$NaN" prices. Implemented proper calculatedItems approach using exact field mapping from converted_pricing_data.csv with enhanced debugging. Fixed data flow from Price List component to generatePriceListHTML function ensuring proper pricePerSheet and pricePerPack calculations. Added comprehensive category mapping system for two-line product display showing category on Line 1 (blue) and product type on Line 2 (bold). Enhanced error handling with fallback values to prevent undefined display in PDF output
- **Customer Management Security Enhancement (2025-07-25)**: Added admin authentication requirements to customer CSV upload functionality. The `/api/admin/upload-customer-data` endpoint now requires both `isAuthenticated` and `requireAdmin` middleware, ensuring only admin users can upload customer data files. Regular users retain full access to customer selection functionality in QuickQuotes and Price List apps through the SearchableCustomerSelect component, maintaining proper security boundaries between data management and data consumption
- **Professional PDF Template Enhancement (2025-07-24)**: Completely redesigned QuickQuotes PDF generation with professional 4S Graphics branding. Features modern blue gradient letterhead with company logo styling, Inter and Roboto font integration, professional three-column quote header layout, enhanced product table with blue gradient headers, stylized total amount section with green accent, and branded footer. PDF now includes proper spacing, rounded corners, shadows, and professional typography matching corporate standards. Maintains customer prefilling functionality while providing polished business document appearance
- **QuickQuotes Customer Selection Integration (2025-07-24)**: Added comprehensive customer selection functionality to QuickQuotes with two-column layout as requested. Left side features SearchableCustomerSelect dropdown with search by name, company, or email. Right side displays clean customer details including contact info, company, phone, and location with appropriate icons. Email Quote and Download PDF buttons now automatically prefill customer email and name respectively, with both buttons disabled until customer is selected. Removed old email dialog system for streamlined user experience
- **Customer Management App Redesign (2025-07-24)**: Completely rebuilt Customer Management app to match ProductPricing Management design with clean, professional interface. Features modern blue gradient theme, stats overview cards showing total customers and companies, upload progress tracking with visual feedback, CSV template download functionality, and comprehensive upload instructions. Fixed API endpoints to use existing backend routes (/api/admin/upload-customer-data and /api/customers). App now provides seamless CSV upload experience with proper error handling and success notifications. Removed all TypeScript compilation errors and implemented proper React Query integration for real-time data updates
- **Admin Panel Cleanup (2025-07-24)**: Removed "Quick Actions" section from Admin Panel containing Customer Management, Product Management, Price Management, and Download Competitor Data CSV buttons. Cleaned up related code including downloadCompetitorPricing function and unused imports (TrendingUp, Package, DollarSign, Link from wouter). Admin panel now focuses solely on user management and database export functionality. Updated layout to single column design and removed redundant navigation notice
- **Dashboard Organization Fix (2025-07-24)**: Removed duplicate ProductPricing Management app from main Applications section on dashboard. App now appears only in Admin Tools section where it belongs, ensuring proper admin-only access control. This eliminates user confusion and maintains proper security boundaries between general user apps and administrative functions
- **File Cleanup and Organization (2025-07-24)**: Cleaned up attached_assets folder by removing outdated files including old screenshots, duplicate CSV files, PDF documents, Excel files, and temporary debug files. Kept essential files: tier_pricing_template.csv (current pricing data), converted_pricing_data.csv, customers_export.csv, PricePAL_All_Product_Data.csv, and most recent screenshot for current issue documentation. This improves project organization and reduces storage usage
- **Enhanced QuickQuotes PDF Format (2025-07-24)**: Updated QuickQuotes PDF generation to match professional format with complete company letterhead (4S Graphics, Inc. with address, phone, website), proper customer information layout, alternating row shading (#ffffff/#f8f9fa), reduced font sizes (9-11px), compact padding, and professional table styling with borders. PDF now includes company details at top, customer info section with quote number and date, and neatly formatted product table with alternating row colors for better readability
- **Enhanced Notification System (2025-07-24)**: Updated notification display to appear in top right corner, reduced size with smaller text (text-xs), and configured 4-second auto-disappear timer. Toast notifications now slide in from right and are more compact (300px max width, reduced padding) for better user experience. Applied to all system notifications including item additions, PDF generation, and email confirmations
- **Simplified Email Quote Template (2025-07-24)**: Updated QuickQuotes email functionality to use simplified, concise email format as requested by user. Email now follows specific template format: "Dear Mr. [Client Name]" greeting, simple product details (Product Name, Type, Size, Item Code, Price/Sheet, Min Order Qty), separator lines, and "Yours truly [User Name]" closing. Maintains native email client functionality while using cleaner, more professional format
- **Complete System Rebuild with ProductPricing Management App (2025-07-24)**: Successfully completed comprehensive rebuild from scratch due to user frustration with reliability issues. Removed all residual code from QuickQuotes, Price List, Pricing Management, and Product Management apps as requested. Created new consolidated "ProductPricing Management app" to replace all previous separate apps. Fixed critical TypeScript compilation errors (reduced from 30 to 2), restored API functionality with proper JSON responses, and maintained database with 60 pricing entries. Updated dashboard to feature new unified ProductPricing Management app with clean foundation ready for new requirements file. Removed all fancy opening screens and complex animations as previously requested. Server successfully running with authentic data sources intact
- **Critical Database Structure Fix (2025-07-24)**: Resolved the core issue where Price List app was looking for data in empty `product_pricing` table instead of the actual `pricing_data` table containing 75 pricing records. Fixed by: 1) Creating missing database structure (product categories, types, sizes, pricing tiers), 2) Populating product_pricing table with data mapped from pricing_data table, 3) Fixing API endpoint to properly transform pricing data format, 4) Adding 60 pricing entries for key tiers (Export, Dealer, Retail). Both Price List and QuickQuotes apps now have access to authentic pricing data through proper database relationships. This addresses the user's concern about "fixing one and breaking another" by ensuring consistent data access across all applications
- **Price List Calculation Logic Fix (2025-07-24)**: Fixed critical Price List app issue showing 0.00 values by implementing proper calculation logic according to specifications. Price List now correctly: 1) Loads all products for selected category from Product Management, 2) Converts product sizes to square meters, 3) Fetches tier-based pricing from Pricing Management, 4) Calculates Price/Sheet = sizeSqM × pricePerSqM, 5) Calculates Price/Pack = pricePerSheet × minQty. Table columns now properly display Size (from Product Management), Item Code (from Product Management), Min Quantity (from Product Management), Price/Sq.Meter (from Pricing Management based on selected tier), Price/Sheet (calculated), and Price/Pack (calculated with retail 99-cent rounding). All calculations use authentic data from management apps with proper NaN protection and error handling
- **QuickQuotes Complete Redesign (2025-07-24)**: Completely rebuilt QuickQuotes app to match the requested two-panel layout design. Left panel features Configure Product section with cascading dropdowns (Product → Product Type → Predefined Size → Quantity). Right panel displays Quote Summary with real-time pricing from all available tiers, showing $/m², Price/Sheet, and Min Order Qty Price with individual Add buttons for each tier. Added Items section shows comprehensive quote table with brand fonts, tier information, and professional layout matching the design specifications. All data sources properly integrated from Product Management (categories/types/sizes), Pricing Management (tiers), and Customer Management (customer selection) apps. Enhanced with PDF generation and email functionality
- **Price List Data Integration with Management Apps (2025-07-24)**: Updated Price List app to properly integrate with Product Management, Pricing Management, and Customer Management apps as requested. Select Product dropdown now populates from Product Management categories, Select Pricing Tier uses data from Pricing Management app, and Select Customer uses Customer Management app through SearchableCustomerSelect component. Fixed TypeScript compatibility issues and ensured proper data flow between all management systems. Price List now seamlessly pulls authentic data from centralized management systems instead of using isolated data sources
- **Customer Management Smart CSV Upload System (2025-07-24)**: Enhanced customer management with intelligent CSV upload functionality that adds new customers and updates existing ones with new information. System matches customers by email address and only updates fields when new data is provided, preventing unnecessary database operations. Added comprehensive feedback showing detailed statistics (customers added, updated, skipped) and user-friendly instructions explaining the add/update logic. Customer data is now fully integrated across QuickQuotes and Price List apps with SearchableCustomerSelect component providing real-time search by name, company, or email. Fixed TypeScript compatibility issues between customer interfaces and streamlined the customer selection experience
- **Enhanced PDF Customer Info Section (2025-07-24)**: Updated PDF generation for both QuickQuotes and Price List apps with improved customer information display. Customer info section now shows: Date (in full format like "July 24, 2025"), Prepared By (user's email), and conditional Prepared for field that only appears when customer name is genuine (excludes test/placeholder names like "Test", "-", "Customer", "Sample"). PDF titles updated to show "QUOTATION" for QuickQuotes app and "PRICE LIST" for Price List app. Maintains all enhanced formatting including categorized product sections, green "Price Per Pack" text, proper company header, and professional footer
- **CSV Upload Data Integrity Fix - RESOLVED (2025-07-24)**: Successfully resolved critical issue where Price Management CSV uploads were only creating 10 records instead of expected 75 due to duplicate endpoint routes and improper batch insert logic. Root cause was identified as two conflicting /api/upload-pricing-csv endpoints where the wrong one (without authentication) was executing individual record creation that caused overwrites. Fixed by disabling old endpoint and implementing proper batch insert with database clearing and composite key preservation. Now correctly uploads all 75 pricing records including 21 "Graffiti Polyester Paper" variations. QuickQuotes, Price List, and Price Management apps now have complete pricing data and function correctly with full product catalog
- **Price Management UI Improvements (2025-07-24)**: Enhanced Price Management interface to reduce confusion with Essential vs All Tiers view toggle, priority-based color coding (key prices in blue), user-friendly column headers, helpful pricing tier explanation guide, and improved visual hierarchy. App now defaults to "Essential" view showing 4 most important pricing tiers (Export, Dealer, Approval, Retail) with option to switch to "All Tiers" for complete 10-tier view. Added clear explanations and better typography for easier data management
- **Removed Fancy Opening Screen Components (2025-07-24)**: Completely removed the fancy opening screen and welcome animation components as requested by user. Deleted WelcomeAnimation.tsx component, Landing.tsx page, removed all welcome animation state and effects from dashboard. Simplified authentication flow to show a basic login button instead of fancy landing page. Removed all complex opening animations including motivational quotes display, particle effects, and welcome messages. Application now loads directly to dashboard after authentication without any fancy intro screens or animations
- **Price Management Database Integration (2025-07-23)**: Fully rebuilt Price Management app with proper database integration. Added complete PostgreSQL schema for pricing data with all pricing tiers (Export, Master Distributor, Dealer, etc.). Implemented CSV upload functionality that parses data into database with proper create/update logic, API endpoints for fetching and editing pricing data, CSV export functionality matching original format, and comprehensive error handling with proper type conversion. Fixed "undefined records" upload issue by ensuring proper CSV parsing and database insertion. App now provides real-time data persistence with search, filtering, and inline editing capabilities
- **Admin Page Simplification (2025-07-23)**: Completely restructured admin page to eliminate upload functionality confusion and focus on core admin tasks. Removed all CSV upload sections for product data, pricing data, customer data, and competitor data from admin page since these are now handled by dedicated Customer Management, Product Management, and Price Management apps. Admin page now contains only User Management (approve/reject pending users) and data export functionality (download all database files as ZIP). Added quick action buttons linking to dedicated management apps and competitor data download. Enhanced Price Management upload button with loading states and better user feedback. This separation of concerns prevents user confusion and consolidates upload functionality into appropriate specialized apps
- **Route Name Update: QuickQuotes Path (2025-07-23)**: Updated application routing from `/quote-calculator` to `/quick-quotes` for consistency with the app's rebrand to "QuickQuotes". This change makes the URL more intuitive for developers and aligns with the current naming convention. Updated all internal links in dashboard, saved quotes, and other components to use the new route. Fixed TypeScript errors in dashboard components with proper type casting for user properties. The quote calculator is now accessible at the more intuitive `/quick-quotes` URL path
- **Font Configuration Migration to Tailwind (2025-07-23)**: Migrated brand-specific font configurations from custom CSS to Tailwind utility classes in tailwind.config.ts. Added fontFamily extensions for font-graffiti (Lobster), font-solvit (Inter 700), font-cliq (Franklin Gothic), font-ele/font-eie (Roboto), font-rang (Inter 400), and additional variants (font-polyester, font-paper, font-blended, font-poly, font-stick). Removed custom .font-* CSS classes from index.css, enabling cleaner Tailwind-based font application throughout the application with consistent utility class naming. Added font preloading in index.html for critical fonts (Lobster, Inter, Roboto, Franklin Gothic) to improve perceived load performance with proper fallbacks for non-JS browsers. Note: .replit file configuration clarification needed for local vs deployment commands
- **Package.json Improvements (2025-07-23)**: Identified multiple package.json enhancement opportunities: (1) Cross-platform environment variable compatibility - installed cross-env package for NODE_ENV=production syntax fix, (2) Build process verification - confirmed esbuild --outdir=dist produces dist/index.js correctly, start script path is accurate, (3) Recommended adding "main": "dist/index.js" field for explicit entry point declaration following Node.js best practices. Manual package.json updates needed due to system protection restrictions
- **TypeScript Schema Testing Enhancement (2025-07-23)**: Converted test_schema.js to test_schema.ts with full TypeScript type safety. Added @types/pg and ts-node dependencies for proper PostgreSQL type definitions. Enhanced with proper interfaces for ColumnInfo and CompetitorPricingRecord, type-safe QueryResult generics, better error handling with proper client connection management, and comprehensive type annotations. Run with "npx tsx test_schema.ts" for TypeScript execution with proper type checking and improved development experience
- **App.tsx Code Cleanup - DRY Principle (2025-07-23)**: Eliminated duplicate JSX in App.tsx by extracting PendingPage component from inline anonymous functions. Removed 40+ lines of redundant JSX for pending user status page that was repeated twice in Switch routes. Created clean, reusable PendingPage component that can be referenced directly in Route components, improving maintainability and following DRY (Don't Repeat Yourself) principle. Code is now more readable and easier to modify
- **Development-Only Console Logging (2025-07-23)**: Wrapped all console.log statements with import.meta.env.DEV conditions to clean up production console output. Applied to App.tsx authentication debugging, useAuth.ts user state logging, and area-pricer.tsx API request debugging. Production builds now have clean browser console while maintaining debugging capabilities in development. Uses Vite's built-in import.meta.env.DEV for efficient tree-shaking in production builds
- **Saved Quotes CSV Export & Enhanced UI (2025-07-23)**: Complete upgrade to saved-quotes.tsx with comprehensive export functionality: (1) Added "Download All CSV" button for bulk export of all quotes with detailed metadata and flattened quote items, (2) Implemented search/filter toolbar for finding quotes by customer name, quote number, email, or status, (3) Added CSV/PDF re-download buttons for individual quotes with proper file generation, (4) Status-based badge coloring (sent=green, pending=yellow, failed=red), (5) Created detailed quote view modal showing full quote items JSON with formatting, (6) Enhanced table layout with action buttons (view, PDF download, CSV download, delete), (7) Added quote count badge and contextual empty states. Uses file-saver library for reliable CSV downloads with timestamped filenames and comprehensive quote data including parsed items summary
- **Saved Quotes Component Comprehensive Fixes (2025-07-23)**: Fixed all critical issues in saved-quotes.tsx including: (1) Added missing queryFn to useQuery with proper API request handling, (2) Enhanced error handling with proper TypeScript error checking using instanceof Error, (3) Replaced window.confirm with professional Dialog component for delete confirmations, (4) Added safe date formatting function with fallback handling for invalid dates, (5) Fixed user role access with proper TypeScript casting, (6) Improved mutation error handling and loading states, (7) Added proper state management for delete dialog. Component now provides robust error handling, better UX, and maintains data integrity
- **Price List API Implementation & Bug Fixes (2025-07-23)**: Fixed all critical issues in price-list.tsx by implementing missing /api/generate-price-list-pdf and /api/generate-price-list-csv endpoints with proper HTML and CSV generation. Enhanced filtering to ensure product types match selected categories explicitly, added NaN protection for square meter calculations, implemented persistent quote number generation (prevents regeneration on every download), fixed unique keys for table rows, and improved category-type relationship filtering. Price list now properly handles size-specific pricing and maintains data integrity throughout the download process
- **Size-Specific Pricing Support (2025-07-23)**: Enhanced product pricing table with size_id foreign key to support size-specific pricing overrides (e.g., 8.5x11" vs 12x18" of same product type can have different prices). Added optional sizeId parameter to pricing API endpoints and updated quote calculator to pass size information when fetching prices. System now checks for size-specific pricing first, then falls back to type-level pricing for maximum flexibility
- **Database Schema Optimization: Timestamp & Integer Fields (2025-07-23)**: Fixed critical database schema issues by converting min_order_qty from VARCHAR(50) to INTEGER with default value of 50, and sent_quotes.created_at from VARCHAR(50) to TIMESTAMP with DEFAULT NOW(). This prevents runtime parsing errors, enables proper date sorting/querying, and improves data integrity. Updated all application code to work with proper data types instead of string parsing. Schema now properly handles timestamps and minimum order quantities with appropriate defaults
- **Blob-Based PDF Download Implementation (2025-07-23)**: Replaced print window approach with blob-based download method for true file saving functionality. Updated PDF generation to use `Blob` API with proper file download using temporary anchor elements. Users now get actual file downloads instead of print dialogs, improving user experience and ensuring quotes are properly saved to disk with correct filenames
- **Enhanced Quote Number Generation System (2025-07-23)**: Implemented robust quote number generation with backend uniqueness validation and customer identifier prefix support. Created server-side generateUniqueQuoteNumber function with retry logic, database validation against sentQuotes table, and fallback mechanisms. Added customer prefix extraction (first 3 letters of company name) for shared quotes with format 4SG-CUS-YYMMDD-XXXX. Includes useQuoteNumber hook for frontend integration, format validation, and utility functions for parsing quote numbers. System ensures uniqueness across all quotes with comprehensive error handling and automatic retries
- **Performance Optimization with Debouncing (2025-07-23)**: Implemented comprehensive debounce system across all search and filter inputs to reduce re-renders and improve UX. Added custom useDebounce hook with 300ms delay for search fields and variable delays for different input types. Applied to SearchableCustomerSelect, Customer Management search/filters, Price Management search, and other key components. This significantly reduces API calls during typing and improves application responsiveness
- **Critical SelectItem Bug Fix (2025-07-23)**: Fixed deployed application crash caused by SelectItem components with empty string values in Customer Management filters. Replaced empty string values with meaningful defaults ("all-cities", "all-provinces", "all-emails") and updated filtering logic to handle these new values properly. Application now deploys successfully without React Select errors
- **File Upload Tracking System (2025-07-22)**: Implemented comprehensive file tracking for CSV uploads in Product Management system. Added database table for file upload metadata including filename, upload timestamp, processing statistics, and active file status. Enhanced Product Management interface to display current data source information with file name, upload date, and processing statistics (records added/updated). System now tracks which CSV file is currently active across all applications (Quote Calculator, Price List, Product Management) and provides visual confirmation to users about data source. Includes automatic data refresh after uploads and proper cache invalidation for real-time updates
- **Critical Bug Fixes & Security Enhancements (2025-07-22)**: Fixed major application stability issues including: (1) Enhanced CSV size parsing regex patterns to handle double quotes (54""x100') and corrupted CSV data with appended text, (2) Improved error handling middleware to prevent server crashes by removing dangerous throw statements after responses, (3) Added comprehensive numeric field validation for competitor pricing to prevent NaN values in database, (4) Strengthened authentication bypasses to be more secure in production environments, (5) Enhanced input validation and sanitization across all API endpoints, (6) Removed pricing tier columns (EXPORT, MASTER_DISTRIBUTOR, etc.) from Product Management app - these are now only visible in Pricing Database for cleaner product catalog view, (7) Fixed CSV category name duplication where ProductType containing redundant category names caused doubled-up display. System now properly cleans type names by removing category prefixes (e.g., "Graffiti Polyester Paper 5mil" becomes "5mil"). These fixes eliminate console warnings, prevent potential server crashes, and ensure data integrity throughout the application
- **Universal CSV Data Append Logic Fix (2025-07-21)**: Enhanced CSV upload merge logic to handle all product categories (GraffitiStick, Solvit, CLiQ, MXP, etc.) with two merge strategies: (1) Update existing products with matching ProductIDs by filling empty fields and updating changed values, (2) Match and update products with empty ProductIDs by comparing ProductName, ProductType, and Size patterns. System now properly handles incomplete data rows across all categories and fills missing pricing data, Item Codes, and other fields. Enhanced logging shows detailed field-by-field updates for comprehensive data population
- **Enhanced CSV Upload Error Handling & Processing (2025-07-21)**: Completely rebuilt product data CSV upload system with robust error handling and detailed feedback. Fixed CSV parsing to handle quoted fields properly (like "12""x18"""), added intelligent merge logic that updates existing products and adds new ones, implemented detailed parsing error reporting with line numbers, enhanced response messages showing exact counts of new/updated/duplicate products, and added comprehensive logging throughout the process. System now gracefully handles malformed CSV data and provides actionable error messages to users
- **Price List Duplicate Prevention & UX Improvements (2025-07-21)**: Fixed critical issue where price list generation was creating duplicate entries in saved quotes by removing automatic quote saving. Added quote number generation and display throughout price list process with visual badges and hash icons. Replaced basic customer dropdown with advanced SearchableCustomerSelect component featuring real-time search by company/contact/email, alphabetical sorting, and integrated "Add New Customer" functionality. Enhanced user experience with clean visual design and immediate feedback
- **Comprehensive File Safety & Logging System (2025-07-19)**: Implemented enterprise-grade file management with complete audit trails. Added cleanup-files.js utility with dry-run preview, automatic backups, and intelligent duplicate detection. All file operations now use safe wrappers with existence checks and detailed logging to file-operations.log. Enhanced server routes with comprehensive error handling and upload/download tracking. Created backup system with timestamped directories and file usage reporting. System identifies and manages duplicate files automatically while preserving latest versions
- **Download Data Deduplication (2025-07-19)**: Fixed admin download functionality to eliminate duplicate files in ZIP archives. System now intelligently selects only main CSV files (customers_export.csv, PricePAL_All_Product_Data.csv, tier_pricing_template.csv) plus the latest area pricing calculation file, reducing download from 10+ duplicate files to 5 essential files. Improves data management and reduces confusion for admin users
- **Motivational Quote Persistence Fix (2025-07-19)**: Removed 5-second auto-hide timer from dashboard motivational quote. Quote now remains visible throughout entire user session as intended, with daily rotation every 6 hours maintained. Cleaned up unused state variables for better code organization
- **Double Dollar Sign Display Fix (2025-07-19)**: Resolved visual bug in Saved Quotes app where Total Amount column showed both dollar sign icon and "$" symbol simultaneously. Removed redundant DollarSign icon component for clean "$334.56" display format
- **Quote Saving Data Integrity Enhancement (2025-07-19)**: Implemented robust handling for quotes with missing sentVia data to prevent blank page errors. Added server-side fallback defaulting to "Not Known" when sentVia field is empty, with proper badge display and error handling for legacy data corruption issues
- **Contemporary Dashboard Icon Design (2025-07-18)**: Redesigned all dashboard app icons with colorful gradients, modern rounded-xl styling, and contemporary icons (Zap, Layers, Sparkles, Target, PieChart, Truck). Added hover animations with scale effects and shadow enhancements for better user experience. Updated both main applications and admin tools with vibrant color schemes including blue-indigo, purple-pink, green-emerald, amber-orange, red-rose, and cyan-sky gradients
- **App Name Changes (2025-07-18)**: Updated "Quote Calculator" app name to "QuickQuotes", "Area Pricer" to "SqM Calculator", and "Competitor Pricing" to "ComIntel" throughout the entire application including dashboard, page titles, and navigation for better branding and user experience
- **Enhanced File Upload System with Smart Duplicate Handling (2025-07-18)**: Completely rebuilt file upload functionality to handle duplicates intelligently. System now adds new records, updates existing records when new data is found in columns, and skips true duplicates. Enhanced user notifications provide detailed feedback showing count of new records added, existing records updated, and duplicates skipped. Applied to customer data, product data, and pricing data uploads with comprehensive CSV parsing and data validation. All uploaded data is immediately available to all users with proper cache clearing
- **Multer Security Update Compatibility (2025-07-18)**: Successfully upgraded to multer v2.0.1 and verified full compatibility with existing file upload functionality. Enhanced error handling and file validation for secure CSV uploads across customer, product, and pricing data management systems
- **Find Duplicates Feature (2025-07-18)**: Added comprehensive duplicate detection functionality to competitor pricing app. New "Find Duplicates" button compares all columns for exact matches and highlights duplicate groups with different colors (red, blue, green, yellow, purple, pink, indigo, orange). Includes smart filtering that works with current filters, toast notifications for results, and toggle controls to show/hide highlighting. Each duplicate group is visually distinct with colored borders and backgrounds
- **Area Pricer Dropdown Fix (2025-07-18)**: Fixed missing dropdown functionality in Area Pricer app by restoring proper Select components for Thickness, Product Kind, and Surface Finish fields. Added comprehensive dropdown options including thickness values (1mil-20mil), product kinds (Vinyl, Paper, Fabric, etc.), and surface finishes (Matte, Gloss, Satin, etc.). Users can now properly select from predefined options instead of typing in text fields
- **Micro-Interaction Feedback System (2025-07-18)**: Implemented comprehensive micro-interaction feedback system with ripple effects, particle animations, success states, loading indicators, and form feedback. Created reusable components (RippleButton, FloatingSuccess, ParticleEffect, etc.) and hooks (useMicroInteractions, useAsyncOperation, useFormFeedback) for enhanced user experience. Integrated MicroFeedbackProvider globally and applied ripple effects to competitor pricing delete buttons
- **Competitor Pricing UI Enhancement (2025-07-18)**: Removed Price/in² and Price/ft² columns from competitor pricing table display and CSV export as requested. Made delete button always visible without horizontal scrolling by using sticky positioning. Added better responsive design with whitespace-nowrap classes and truncated notes with hover tooltips for better table layout
- **Fixed ADD TO COMP INFO Button Functionality (2025-07-18)**: Completely rewrote the ADD TO COMP INFO button in Area Pricer with simplified, inline implementation using direct fetch calls instead of React Query mutations. Removed complex function chains and conditional rendering issues that were preventing button clicks from registering. Button now provides immediate feedback, clear error messages, and successfully adds calculation data to competitor pricing database with proper field validation and data conversion
- **Fixed Competitor Pricing Data Type Issues (2025-07-18)**: Resolved critical blank screen issue in Competitor Pricing Intelligence app caused by database storing numeric values as strings while frontend expected numbers. Fixed with parseFloat() conversions for all numeric fields (inputPrice, pricePerSqIn, pricePerSqFt, pricePerSqMeter), updated TypeScript interfaces to handle mixed data types, and enhanced price filtering logic. Authentication system working properly with development bypass for testing environments
- **Complete Competitor Pricing App Rebuild (2025-07-17)**: Completely rebuilt the Competitor Pricing Intelligence app from scratch with clean, robust architecture. New implementation features proper authentication flow with clear loading states, comprehensive error handling that prevents blank pages, streamlined data filtering and CSV export functionality, and reliable file upload system. The app now gracefully handles all authentication states, data loading errors, and user interactions without crashes or blank pages. Simplified code structure for better maintainability and performance
- **CSV Upload System Rebuild (2025-07-17)**: Completely rebuilt CSV upload system with comprehensive debugging and error handling to prevent page crashes during file uploads. Added detailed logging throughout the upload process, improved CSV parsing with proper quote handling, and enhanced frontend error handling with try-catch blocks. System now provides clear feedback for upload success/failure and handles malformed CSV data gracefully without crashing the application
- **Automatic Competitor Data Collection (2025-07-17)**: Enhanced Area Pricing Calculator to automatically save all calculations to the shared competitor pricing database. Now when users click "Add to Sheet", their calculations are automatically shared with all users for collective intelligence. Added admin CSV upload functionality for competitor pricing data with flexible header mapping. Updated data storage to use shared database instead of in-memory storage for persistent data across all users
- **Server-Side Deployment Fix (2025-07-17)**: Fixed critical deployment issue where localStorage migration was causing "fetch is not a valid HTTP method" errors in server-side rendering environment. Added proper browser environment checks to ensure localStorage operations only run client-side. Migration function now safely handles both browser and server environments with error handling for corrupted data
- **Competitor Pricing Database Migration (2025-07-17)**: Successfully migrated competitor pricing data from localStorage to server-side PostgreSQL database with shared visibility across all users. Implemented automatic data migration functionality that preserves existing localStorage data while transitioning to centralized storage. Updated all apps (Competitor Pricing, Area Pricer, Admin panel) to use server API instead of localStorage
- **Shipping Calculator Email Enhancement (2025-07-16)**: Successfully implemented "Email This" functionality for individual shipping rates in Shipping Calculator app. Each shipping option row now has a dedicated "Email This" button with mail icon that opens user's default email client with pre-composed message containing complete shipping details: route information, package specifications, selected service details, pricing, and any calculation notes. Fixed React Query deprecation warnings by replacing `cacheTime` with `gcTime` across all query configurations. Email composition includes professional signature from 4S Graphics and toast notification for user feedback
- **Adaptive Login Animation System (2025-07-16)**: Implemented intelligent login animation system that adapts to user experience level. New users (≤10 logins) get full 7-second welcome animation with login count indicator and snail icon. Experienced users (10-100 logins) get faster 60% speed animation. Expert users (100+ logins) skip animation entirely and go straight to dashboard. Added sarcastic "welcome back" animation with orange styling and clock icon for users returning after 30+ minutes of inactivity. Motivational quotes now show once per login session and auto-hide after 5 seconds. Created EmailCelebrationAnimation component for quote sending success feedback
- **User Activity Tracking (2025-07-16)**: Added loginCount and lastLoginDate fields to user schema with automatic incrementation on each login. Implemented activity tracking with localStorage to detect user inactivity periods for personalized return experience. Updated authentication system to track user engagement patterns for adaptive UX optimization
- **Price List Column Structure Update (2025-07-16)**: Updated Price List app with proper three-column pricing structure: (1) Price/Sq.M shows original CSV pricing data, (2) Price/Sheet shows actual price per individual sheet (sq.m price × square meters), (3) Price Per Pack shows price per sheet × minimum order quantity with 99-cent rounding applied only to retail pricing tier
- **Role-Based Pricing Tier Filtering (2025-07-16)**: Implemented comprehensive role-based pricing tier visibility across Quote Calculator and Price List apps. Admin users see all tiers, Santiago sees only Approval/Stage tiers (1, 15, 2, 25), Patricio sees Santiago's tiers plus Dealer/Dealer2/Master Distributor tiers. Added zero-price tier filtering that automatically hides tiers with zero pricing values from CSV data. Created roleBasedTiers.ts utility for consistent tier filtering with getUserRoleFromEmail function for role determination
- **Retail Pricing Rounding (2025-07-16)**: Implemented 99-cent rounding for retail pricing tiers across Quote Calculator and Price List apps. When "Retail" tier is selected, prices are automatically rounded to .99 cents (e.g., 17.89 becomes 17.99, 16.40 becomes 16.99). Applied to all price displays including web interface, PDF exports, and CSV downloads for consistent pricing presentation
- **Shipping Calculator App (2025-07-16)**: Built comprehensive shipping calculator app inspired by SpeedShip functionality. Features include package dimension and weight input, automatic dimensional weight calculation, zone-based pricing, multiple service levels (Ground, Priority, Express, Overnight), surcharge calculations for high-value packages, calculation history with CSV export, and responsive design. Added to dashboard with truck icon and integrated into main navigation. App starts fresh each time user visits (no data persistence) but maintains session history for current use and CSV export functionality
- **Icon and Font Enhancement (2025-07-16)**: Updated all CSV export buttons across entire application to use Sheet icon instead of Download icon for better user experience. Fixed Quote Calculator product dropdown to properly apply brand-specific fonts to category and type selection lists. Completed font implementation: "Graffiti" uses Lobster font, "Polyester", "Paper", "Blended", "Poly", "Stick" use Roboto font, "Solvit" uses Inter 700, "CLiQ" uses Franklin Gothic, "Rang" uses Inter 400, "eLe/EiE" use Roboto, all other text uses Inter font. Font styling applies to individual words within product names for precise branding across all apps
- **UI/UX Enhancement (2025-07-16)**: Removed Configure button from Price List interface for cleaner design. Updated button styling across entire app: Download PDF buttons now use blue color with PDF icon, Export CSV buttons use green color with Excel icon. Implemented brand-specific font requirements: "Graffiti" uses Lobster font, "Polyester", "Paper", "Blended", "Poly", "Stick" use Roboto font, "Solvit" uses Inter 700, "CLiQ" uses Franklin Gothic, "Rang" uses Inter 400, "eLe/EiE" use Roboto, all other text uses Inter font. Font styling applies to individual words within product names for precise branding
- **PDF Generation Enhancement (2025-07-16)**: Fixed Price List PDF generation with proper pagination (page X of Y), 8.5x11 portrait orientation, updated column headers to "Price per Sheet" and added "Price Per Pack" column. Implemented automatic file saving for product updates and proper CSS print media queries for professional formatting. Fixed customer display to show selected customer's company name or hide "Price List for:" line when no customer is selected. Added "No Company Name Found" error message for PDF downloads without customer selection. Removed grayed out "Area Pricer" and "Data Guide" apps from dashboard to avoid confusion. Moved Customer Management and Product Management apps to Admin Tools section for better organization
- **Product Management App (2025-07-16)**: Created comprehensive Product Management app with Excel-like interface. Features include product catalog display with categories, types, sizes, dimensions, and pricing tiers. Admin-only editing capabilities with inline cell editing for dimensions, item codes, and quantities. Includes category filtering, upload functionality placeholder, and automatic CSV file saving. Added to dashboard and router with proper API endpoints
- **Customer Management Integration (2025-07-16)**: Added Customer Management card to main dashboard and implemented complete CRUD API endpoints for customer operations. Fixed Price List download to use selected customer data instead of manual input
- **Customer Selection Enhancement (2025-07-16)**: Added customer selection/creation functionality to Price List app. Users can now select existing customers or create new ones with Company Name, Address, City, State, Zip, Contact Name, Phone & Email. Removed client name input field from download dialog, using selected customer data instead. Fixed PDF print button visibility during download to eliminate "Print to PDF" text in generated files
- **Dimensions Column Removal (2025-07-16)**: Completely removed Dimensions column from Price List web display, PDF downloads, and CSV exports as requested. Table now shows Size, Item Code, Min Qty, Price/Sq.M, and Total Price columns only
- **Price List Download Enhancement (2025-07-16)**: Removed pricing tier information from PDF and CSV downloads, updated filenames to exclude tier name, and implemented automatic saving of generated files to Saved Quotes system. Fixed PDF generation to use HTML download approach for better browser compatibility
- **Dimensions CSV Export Fix (2025-07-16)**: Updated competitor pricing database CSV export to separate dimensions into two columns (Width and Height/Length) instead of single combined cell. Enhanced data structure to store width, length, and unit separately for better parsing accuracy
- **Code Cleanup (2025-07-16)**: Removed unused DatabaseStorage class, mock data, and unnecessary imports. Cleaned up user initialization to rely only on authenticated users with proper pre-approval system
- **Dashboard Redesign (2025-07-15)**: Redesigned dashboard with all apps in one horizontal line and separate admin section with download functionality for all databases
- **Email Product Name Fix (2025-07-15)**: Fixed product name duplication in email composition to show only product type instead of brand + type combination
- **Product Code Integration (2025-07-15)**: Added product code display to quote items table, email composition, and PDF generation. Updated all QuoteItem interfaces to include itemCode field from productSizes schema
- **Email Signature Removal (2025-07-15)**: Removed company signature section from email composition at user request while keeping product codes in quote items
- **Minimum Order Quantity Logic (2025-07-15)**: Implemented smart minimum order quantity display - Min Order Qty column appears only when needed, calculations use minimum when user quantity is below threshold
- **Authentication System (2025-07-15)**: Implemented Replit Auth with domain restriction to @4sgraphics.com emails only. Added admin approval workflow with aneesh@4sgraphics.com, oscar@4sgraphics.com, and shiva@4sgraphics.com as admins. Pre-approved users include santiago@4sgraphics.com, patricio@4sgraphics.com, and remy@4sgraphics.com as regular users. All other users require manual admin approval
- **New Dashboard Design (2025-07-15)**: Created employee portal dashboard matching user requirements with tool grid layout, proper branding, and personalized welcome message
- **Database Integration (2025-07-15)**: Added PostgreSQL database with Drizzle ORM for user management and session storage
- **PDF Typography (2025-07-15)**: Updated PDF styling to use Roboto font - 15px for company name, 10px for all other text with professional alternating row colors
- **Performance Optimization (2025-07-15)**: Implemented client-side and server-side caching to improve API response times and reduce loading delays
- **Role Management Removal (2025-07-15)**: Removed role assignment functionality from admin panel. Only aneesh@4sgraphics.com and oscar@4sgraphics.com have admin roles, all other users are regular users

## User Preferences

Preferred communication style: Simple, everyday language.

## System Architecture

### Frontend Architecture
- **Framework**: React with TypeScript
- **Routing**: Wouter for client-side routing
- **State Management**: TanStack Query (React Query) for server state
- **UI Components**: shadcn/ui component library built on Radix UI
- **Styling**: Tailwind CSS with CSS custom properties for theming
- **Build Tool**: Vite for development and production builds

### Backend Architecture
- **Runtime**: Node.js with Express.js
- **Language**: TypeScript with ESM modules
- **API Style**: RESTful API endpoints
- **Database**: PostgreSQL with Drizzle ORM
- **Session Management**: PostgreSQL session store (connect-pg-simple)
- **Development**: tsx for TypeScript execution

### Database Architecture
- **ORM**: Drizzle ORM with PostgreSQL dialect
- **Migration**: Drizzle Kit for schema migrations
- **Connection**: Neon Database serverless connection
- **Schema Location**: `/shared/schema.ts` for shared types between frontend and backend

## Key Components

### Database Schema
The application manages a hierarchical product catalog with pricing tiers:
- **Product Categories**: Top-level product groupings
- **Product Types**: Subcategories within each category
- **Product Sizes**: Specific size options with dimensions and square meter calculations
- **Pricing Tiers**: Tiered pricing based on square meter ranges
- **Users**: Basic user authentication system

### Frontend Components
- **Quote Calculator**: Main application interface for calculating product quotes
- **UI Components**: Comprehensive shadcn/ui component library including forms, dialogs, cards, and navigation
- **Responsive Design**: Mobile-first approach with Tailwind CSS breakpoints

### Backend Services
- **Storage Interface**: Abstracted storage layer with both in-memory and database implementations
- **API Routes**: RESTful endpoints for product categories, types, sizes, and pricing
- **Error Handling**: Centralized error handling middleware
- **Development Tools**: Request logging and error overlay for development

## Data Flow

1. **User Interaction**: User selects product category, type, and size through the quote calculator interface
2. **API Requests**: Frontend makes requests to backend REST endpoints using TanStack Query
3. **Database Queries**: Backend queries PostgreSQL through Drizzle ORM
4. **Price Calculation**: System calculates pricing based on square meters and pricing tiers
5. **Response**: Formatted data returned to frontend for display

### API Endpoints
- `GET /api/product-categories` - Fetch all product categories
- `GET /api/product-types/:categoryId` - Fetch product types for a category
- `GET /api/product-sizes/:typeId` - Fetch product sizes for a type
- `GET /api/pricing-tiers` - Fetch all pricing tiers
- `POST /api/calculate-price` - Calculate price for custom dimensions

## External Dependencies

### Frontend Dependencies
- **React Ecosystem**: React, React DOM, React Query
- **UI Libraries**: Radix UI primitives, Lucide React icons
- **Styling**: Tailwind CSS, class-variance-authority for component variants
- **Forms**: React Hook Form with Zod validation
- **Utilities**: clsx, tailwind-merge, date-fns

### Backend Dependencies
- **Core**: Express.js, Node.js types
- **Database**: Drizzle ORM, Neon Database serverless client
- **Session**: connect-pg-simple for PostgreSQL session storage
- **Validation**: Zod for schema validation
- **Development**: tsx for TypeScript execution

### Development Tools
- **Build**: Vite, esbuild for production builds
- **TypeScript**: Strict configuration with path mapping
- **Linting**: ESLint configuration (implied by shadcn/ui setup)
- **Database**: Drizzle Kit for migrations and schema management

## Deployment Strategy

### Build Process
1. **Frontend Build**: Vite builds React application to `/dist/public`
2. **Backend Build**: esbuild bundles Express server to `/dist/index.js`
3. **Database Setup**: Drizzle migrations applied via `npm run db:push`

### Environment Configuration
- **Development**: Uses NODE_ENV=development with tsx for hot reloading
- **Production**: NODE_ENV=production with compiled JavaScript
- **Database**: Requires DATABASE_URL environment variable for PostgreSQL connection

### Deployment Architecture
- **Frontend**: Static assets served from `/dist/public`
- **Backend**: Express server handles API routes and serves static files
- **Database**: PostgreSQL database (configured for Neon serverless)
- **Session Storage**: PostgreSQL-backed sessions for authentication

### Development vs Production
- **Development**: Vite dev server with HMR, runtime error overlay
- **Production**: Static file serving with Express, compiled assets
- **Database**: Same PostgreSQL setup for both environments
- **Error Handling**: Enhanced error reporting in development mode

The application is designed to be deployed on platforms like Replit, Vercel, or similar Node.js hosting services with PostgreSQL database support.